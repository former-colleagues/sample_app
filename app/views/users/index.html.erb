<% provide(:title, 'All users') %>
<h1>All users</h1>

<% @path = users_path %>
<%= render 'shared/search_form' %>

<%= will_paginate %>

<ul class="users">
		<%= render @users %>
</ul>

<%= will_paginate %>